{
  "private": true,
  "scripts": {
    "dev": "concurrently -n FRONT,BACK -c magenta,cyan \"npm --prefix ./frontend run dev\" \"npm --prefix ./backend run dev\"",
    "build": "npm --prefix ./frontend run build && npm --prefix ./backend run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "echarts": "^6.0.0"
  }
}
