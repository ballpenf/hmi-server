import { Router } from "express";
import * as mainController from "../controllers/mainController.js";

const router = Router();

//router.get("/getTooltestData", mainController.getTooltestData);
//router.post("/setTooltestData", mainController.setTooltestData);
//router.post("/getValues", mainController.getValues);

export default router;
